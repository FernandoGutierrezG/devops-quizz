name: Test App

on:
  workflow_call:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Run Unit Tests  
      run: |
        pwd
        echo "Testing App ..."
